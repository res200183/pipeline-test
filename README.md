
## AWS CodePipeline CI/CD Demo

This project demonstrates a simple CI/CD pipeline using:

- GitHub (source code)
- AWS CodePipeline
- AWS CodeBuild

When code is pushed to GitHub, the pipeline automatically starts and runs the build process.

### Pipeline Flow

Developer push → GitHub → CodePipeline → CodeBuild
